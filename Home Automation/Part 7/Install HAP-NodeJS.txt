curl -sL https://deb.nodesource.com/setup_4.x | sudo -E bash -

sudo apt-get install git nodejs libavahi-compat-libdnssd-dev node-gyp

git clone https://github.com/KhaosT/HAP-NodeJS.git

cd HAP-NodeJS

sudo npm install

sudo npm install mqtt

